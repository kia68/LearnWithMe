package de.optadata.odil.learnwithme.content.internal.domain

/** Herkunft/Format einer [Source] (B1-B4). */
enum class SourceKind {
    PDF,
    DOCX,
    URL,
    HTML_SNIPPET,
    TEXT,
    EPUB,
}
